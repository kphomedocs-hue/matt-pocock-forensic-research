# Contradiction Register

Statuses: `OPEN / RESOLVED / ACCEPTED DESIGN / UNKNOWN`.
Defect-state labels: `CURRENT / FIXED / PARTIALLY FIXED / DOCS STALE / UNKNOWN`.

## Current contradictions / enforcement gaps

| ID | Area | Evidence A | Evidence B | Classification | Status | Notes |
|---|---|---|---|---|---|---|
| CT-001 | TDD description | Frozen `CHANGELOG.md` (MP-0025) records that v1.1.0 removed refactor from TDD and moved it to review; frozen operative `tdd/SKILL.md` (MP-0100) explicitly says red → green and that refactoring is not part of the loop. | Frozen root `README.md` (MP-0029), Engineering README (MP-0061), and Codex metadata `tdd/agents/openai.yaml` (MP-0101) still describe TDD as red-green-refactor. | DOCS / METADATA STALE | OPEN | Historical intent and current operative source agree; multiple user/distribution-facing descriptions remain stale. |
| CT-002 | ask-matt counts | Frozen plugin manifest (MP-0018) lists 25 promoted skills; metadata census established 14 user-invoked / 11 model-invoked in the promoted set. | Frozen `docs/engineering/ask-matt.md` (MP-0030) says “Thirteen of the plugin's twenty-two skills carry the flag.” | DOCS STALE | OPEN | Quantitative statement is directly stale in the frozen docs. |
| CT-003 | ask-matt verification behavior | Frozen `docs/engineering/ask-matt.md` (MP-0030) says success means behavior claims show trace evidence of reading the target `SKILL.md`, and calls the router a secondary source. | Frozen operative `skills/engineering/ask-matt/SKILL.md` (MP-0063) contains no mandatory step to open/read the target skill source before making load-bearing claims. | ENFORCEMENT GAP | OPEN | Router summaries can function as unchecked secondary-source assertions. |
| CT-004 | package lock version | Frozen package.json MP-0056 is version 1.2.3. | Frozen package-lock.json MP-0055 retains root version 0.0.0 in both top-level and root-package metadata. | METADATA DRIFT | OPEN | History shows lockfile was never regenerated after bootstrap. Direct local npm observation now shows npm pack uses package.json version 1.2.3 and package-lock-only normalization rewrites both root lock version fields to 1.2.3; live release/CI consumer behavior remains open under B-029. Version sync MP-0059 does not touch package-lock. |
| CT-005 | teach glossary linkage | Frozen support file MP-0152 `GLOSSARY-FORMAT.md` exists and defines a substantial canonical glossary format. Exhaustive reference extraction finds one incoming reference, from frozen human docs MP-0051 `docs/productivity/teach.md`. | Frozen operative Teach source MP-0156 discusses glossaries as essential but does not reference `GLOSSARY-FORMAT.md`; MP-0051 explicitly says the skill ships the file but `SKILL.md` no longer links to it. | OPERATIVE LINKAGE GAP / DOCUMENTED STALE SUPPORT | OPEN | Source-tree orphan hypothesis is disproved; functional linkage gap remains. |
| CT-006 | teach workspace root | Teach source uses relative paths both for co-located support and user workspace state. | Issue #377 reports files being written into installed skill directory. | RUNTIME AMBIGUITY | OPEN | Needs harness/runtime reproduction to move beyond reported evidence. |
| CT-007 | diagnosing-bugs redaction | Frozen operative diagnosing-bugs source MP-0071 contains an explicit `Redact` section requiring `<REDACTED>`, env-var-based loops, and minimal quoted artifact lines; frozen CHANGELOG MP-0025 records the same behavior shipping in v1.2.3. | Frozen `docs/engineering/diagnosing-bugs.md` (MP-0033) still says artifact/output redaction is open and unimplemented. | DOCS STALE | OPEN | Current operative source and release history agree; human docs are stale. |
| CT-008 | architecture report portability | Frozen `HTML-REPORT.md` MP-0082 and operative architecture skill MP-0083 call the artifact a self-contained HTML file but load Tailwind and Mermaid from CDNs. | Frozen human docs report silent degradation offline/locked-down. | RUNTIME PORTABILITY GAP | OPEN | Self-contained in file packaging, not dependency independence. |
| CT-009 | diagnosing-bugs architecture handoff | Frozen changeset MP-0015 says the autonomous `diagnosing-bugs` → `improve-codebase-architecture` handoff was removed outright; frozen operative diagnosing-bugs source MP-0071 contains no such handoff. | Frozen diagnosing-bugs docs MP-0033 and operative ask-matt router MP-0063 still describe that handoff. | DOCS / ROUTER STALE | OPEN | Operative source confirms removal; two secondary surfaces retain removed behavior. |
| CT-010 | implement → code-review ordering | Frozen operative implement source MP-0080 explicitly says run `/code-review` once done, then commit. | Frozen operative code-review source MP-0065 reviews only `git diff <fixed-point>...HEAD` and fails early on empty diff; staged/working-tree-only changes are outside that comparison. | WORKFLOW ENFORCEMENT GAP | OPEN | A run with no interim commit can invoke review before the implementation exists in HEAD. |
| CT-011 | retro maturity description | Frozen `skills/in-progress/README.md` MP-0117 labels `retro` as `STUB: design notes only, not functional yet`. | Frozen operative `retro/SKILL.md` MP-0124 contains a complete multi-step retrospective workflow, categories, file guidance, and an operative call to `writing-for-agents`. | BUCKET DOCS STALE | OPEN | Bucket README materially understates implemented behavior. |
| CT-012 | setup-ts-deep-modules rule count / freedom | Frozen operative MP-0126 says “Four rules, all `error`,” defines rule 2 as “a package's own files import each other freely,” later says to merge “the four rules,” and declares config complete when “the four forbidden rules are present.” | Frozen executable config MP-0128 contains **five** `forbidden` entries, all `severity: "error"`. The additional `tests-folder-is-private` rule prevents any non-test importer — including same-package implementation code — from importing a package's `tests/` folder. | OPERATIVE / CONFIG CONTRACT MISMATCH | OPEN | Not merely a split implementation of one boundary: the fifth rule adds a separately named restriction and creates an exception to the prose's unconditional intra-package-freedom claim. |
| CT-013 | Wayfinder planning-only override authority | Frozen operative Wayfinder MP-0112 says planning is the default and absent an override it should produce decisions, not deliverables; it also allows an effort to override this in map `Notes`, carrying execution into the map. | The same source tells the agent, while charting, to create the map with `Destination and Notes filled in`; no separate rule requires an execution-carrying Notes override to be supplied or confirmed by the human. | PROMPT ENFORCEMENT WEAKNESS / SELF-AUTHORED OVERRIDE | OPEN | This is not a direct textual contradiction: the override is documented. The weakness is authority separation — the same agent subject to the planning guard can author the field that relaxes it. |
| CT-014 | implement-spec post-review fix closure | Frozen operative `implement-spec/SKILL.md` MP-0120 says that once all tickets are complete, run `/code-review`, then “Fix all issues raised by the code review in a single implementer subagent.” | The next step is immediately “Mark the PR as ready for review.” There is no required second `/code-review`, targeted verification of the reported findings, or equivalent acceptance gate after the fix subagent changes the branch. | WORKFLOW ENFORCEMENT GAP | OPEN | The final review checks the pre-fix state, while the post-review repair delta is not itself required to be revalidated before readiness. |
| CT-015 | to-spec parent `ready-for-agent` routing | Frozen operative `to-spec/SKILL.md` MP-0104 publishes the parent spec issue and automatically applies the `ready-for-agent` triage label with “no need for additional triage.” | Frozen human docs MP-0043 explicitly warn that AFK agents polling `ready-for-agent` cannot see the intended “input designation, not a work order” distinction and may build the entire spec in one run; the documented workaround is to exclude the parent or strip the label after `/to-tickets`. Frozen operative `to-tickets/SKILL.md` MP-0106 says not to close or modify the parent issue, so that repair is not part of the downstream workflow. | WORKFLOW / ROUTING ENFORCEMENT GAP | OPEN | One label carries two meanings: “complete planning input” and “agent-executable work.” The operative chain does not mechanically disambiguate the parent spec from its executable ticket slices. |
| CT-016 | to-tickets acceptance criteria falsifiability | Frozen operative `to-tickets/SKILL.md` MP-0106 requires acceptance-criteria checkboxes in each ticket but does not require a falsifying observation or that each criterion fail at the implementer's starting commit. | Frozen human docs MP-0044 explicitly say the template “asks for criteria and says nothing about whether they can fail,” describing criteria that are already true at base, depend on another ticket, or merely restate the request; the docs recommend checking falsifiability and confirming red-at-base by hand. | ACCEPTANCE-CRITERIA ENFORCEMENT GAP | OPEN | The human docs already identify the missing operative quality gate; vertical slicing reduces risk but does not enforce criterion falsifiability. |
| CT-017 | implement ticket lifecycle / frontier progression | Frozen operative `to-tickets/SKILL.md` MP-0106 defines the frontier from blocker completion/closure and publishes dependency edges accordingly. | Frozen operative `implement/SKILL.md` MP-0080 ends at code-review and commit with no ticket-close or acceptance-checkbox reconciliation step. Frozen human docs MP-0036 explicitly say this is expected and warn that on dependency chains “If nothing gets closed, nothing ever becomes visibly unblocked.” | WORKFLOW LIFECYCLE / FRONTIER ENFORCEMENT GAP | OPEN | The producer models execution as a dependency graph whose frontier advances on completion, but the intended per-ticket executor does not publish completion back into that graph. Manual lifecycle repair is required between runs. |
| CT-018 | triage `ready-for-agent` brief invariant | Frozen `triage/AGENT-BRIEF.md` MP-0108 says an agent brief is posted when an issue or PR moves to `ready-for-agent` and is the authoritative specification/contract an AFK agent works from. Frozen normal triage outcome MP-0110 likewise requires `ready-for-agent` to post an agent brief comment. | The same operative triage source's **Quick state override** says to trust “move #42 to ready-for-agent,” apply the role directly, then—if no grilling occurred—only ask whether the maintainer wants to write an agent brief. The state can therefore be applied while the brief remains optional/absent. | WORKFLOW STATE-INVARIANT GAP | OPEN | The fast path can create a `ready-for-agent` item without the durable contract the support rules define as authoritative for that state. |

| CT-019 | migrate-to-shoehorn assertion discovery — audit correction | Frozen operative MP-0139 promises both `as Type` and `as unknown as Type` and documents `grep -r " as [A-Z]" --include="*.test.ts" --include="*.spec.ts"` as its discovery step. | Exact frozen-command reproduction `CT019-R1` searched a fixture containing both forms. Grep returned the double assertion because `as unknown as Request` contains a later ` as Request` substring that matches the pattern; a lowercase-target control did not match. | AUDIT FALSE POSITIVE / NO SOURCE DISCOVERY GAP | RESOLVED | Earlier audit reasoning incorrectly treated the pattern as if it had to match the first `as` token. The source discovery command was not defective on the advertised double-assertion case. The correction is retained rather than deleting CT-019. |
| CT-020 | scaffold-exercises allowed variant set | Frozen operative scaffold-exercises MP-0141 says each exercise needs at least one of `problem/`, `solution/`, or `explainer/`, which makes a solution-only exercise valid by the stated rule. | The same source's lint-rules summary says at least one of `problem/`, `explainer/`, or `explainer.1/` must exist; `solution/` is absent from that primary-variant acceptance set, so a solution-only scaffold satisfies the prose rule but fails the described linter rule. | OPERATIVE / LINTER CONTRACT MISMATCH | OPEN | The contradiction is internal to one operative skill and can produce a scaffold that follows one section while failing the mandated completion linter. |

## Normalized closure overlay

This overlay adds the canonical fields defined in `00_RESEARCH_SCHEMA.md` without redefining primary contradiction IDs.

| Ref | Origin | Defect state | Impact | Resolution test / next evidence |
|---|---|---|---|---|
| ↳ CT-001 | SOURCE_REPO | DOCS STALE | User-facing and Codex-facing TDD semantics can disagree with operative behavior. | Confirm all frozen/current secondary surfaces describe red → green only, or document an intentional semantic split. |
| ↳ CT-002 | SOURCE_REPO | DOCS STALE | Router documentation reports wrong plugin/invocation counts. | Recompute promoted/invocation counts mechanically and verify the docs match the generated set. |
| ↳ CT-003 | SOURCE_REPO | CURRENT | Router can make behavior claims without proving the target source was read. | Show an operative mandatory target-source verification step, or classify the gap as accepted design. |
| ↳ CT-004 | SOURCE_REPO | PARTIALLY FIXED | Package metadata contains two version truths; downstream tooling effect is uncertain. | Trace package-lock consumers/runtime and determine whether stale root version has practical effect. |
| ↳ CT-005 | SOURCE_REPO | CURRENT | A canonical glossary format exists and is documented, but Teach does not load/reference it operatively. | Wire the format into Teach, retire it, or explicitly accept optional/manual behavior. |
| ↳ CT-006 | SOURCE_REPO | UNKNOWN | Relative-path ambiguity may cause writes into installed skill state. | Reproduce in supported harness/runtime or obtain equivalent direct evidence of workspace-root resolution. |
| ↳ CT-007 | SOURCE_REPO | DOCS STALE | Human docs understate shipped redaction safeguards. | Verify current docs align with operative redaction behavior and release history. |
| ↳ CT-008 | SOURCE_REPO | CURRENT | “Self-contained” report can degrade when CDN access is unavailable. | Define self-contained semantics and test offline/locked-down rendering behavior. |
| ↳ CT-009 | SOURCE_REPO | DOCS STALE | Removed architecture handoff remains advertised by docs/router. | Verify all current secondary surfaces remove the deleted handoff or explicitly mark it historical. |
| ↳ CT-010 | SOURCE_REPO | CURRENT | Review can run before implementation exists in HEAD, yielding an empty committed diff. | Reproduce ordering path and verify either interim commit requirement or review support for staged/working-tree changes. |
| ↳ CT-011 | SOURCE_REPO | DOCS STALE | Bucket README materially misstates `retro` maturity. | Align bucket description with operative skill state. |
| ↳ CT-012 | SOURCE_REPO | CURRENT | Skill prose understates the shipped enforcement surface and falsely states unrestricted intra-package freedom; a reimplementation/merge following the prose can omit the tests-folder-private restriction. | Either document five error-level forbidden rules and the tests-folder exception, or restructure the config/prose so the claimed four-rule model is exact and the completion check matches it. |
| ↳ CT-013 | SOURCE_REPO | CURRENT | The planning-only guard can be relaxed through a field the same agent is instructed to author, so the override has no explicit independent authorization boundary. | Require execution override to be human-supplied/confirmed, demonstrate another enforced authority boundary, or classify self-authored Notes override as accepted design. |
| ↳ CT-014 | SOURCE_REPO | CURRENT | A fix subagent can alter the branch after the only required code-review pass, and the workflow can then mark that changed branch ready without proving the review findings were actually resolved or that the repair introduced no new findings. | Require a second `/code-review`, a finding-by-finding verification gate, or another deterministic acceptance check after the post-review fix subagent and before marking the PR ready. |
| ↳ CT-015 | SOURCE_REPO | CURRENT | Parent specs can enter the same `ready-for-agent` polling surface as executable ticket slices, so an AFK worker can select the wrong unit of work. | Use a distinct parent/spec state, strip `ready-for-agent` as part of ticketization, or make dispatchers mechanically distinguish parent specs from executable tickets. |
| ↳ CT-016 | SOURCE_REPO | CURRENT | Tickets can carry acceptance criteria that provide no red/green proof, are already satisfied before work starts, or depend on another ticket's output. | Require every criterion to name a falsifying observation and prove it is false at the ticket's base state unless explicitly justified. |
| ↳ CT-017 | SOURCE_REPO | CURRENT | Completed implementation work can leave the tracker dependency graph unchanged, preventing the next frontier from becoming visibly unblocked without manual intervention. | Make `/implement` or an explicit completion step reconcile acceptance criteria and close/update the ticket before frontier recomputation; then verify dependent tickets become unblocked. |
| ↳ CT-018 | SOURCE_REPO | CURRENT | `ready-for-agent` can signal AFK implementability while the authoritative agent contract is absent, leaving the next agent to infer scope from weaker issue context. | Make an agent brief mandatory before/appended atomically with `ready-for-agent`, or explicitly redefine the quick-override state semantics and downstream consumer contract. |

| ↳ CT-019 | AUDIT_TOOLING | FIXED | The audit temporarily overstated the source defect surface by registering a discovery gap that the exact command does not have. The frozen source itself was unchanged. | Reproduced exact documented grep against simple, double, and lowercase-control fixtures; both advertised forms are found and the negative control is not. Discovery hypothesis closed; migration-edit correctness remains a separate later test. |
| ↳ CT-020 | SOURCE_REPO | CURRENT | An implementer can create a solution-only exercise that appears valid under the variant guidance but cannot satisfy the stated lint gate. | Make the allowed-variant prose and actual linter acceptance set identical, then test a solution-only fixture and document whether it must pass or fail. |

## Resolved historical defects retained for pattern analysis

| ID | Area | Defect | Resolution | State |
|---|---|---|---|---|
| CT-H01 | Cross-skill invocation | PR #878 mechanically rewrote soft references into Skill-tool calls including user-invoked targets. | PR #880 restored human-run preconditions and added invocation-type carve-out. | FIXED |
| CT-H02 | Router completeness | ask-matt historically missed multiple shipped skills. | Later coherence passes added missing routes and governance rule to re-check router on skill changes. | FIXED, recurrence risk remains |
| CT-H03 | Plugin/package version | plugin.json manually advanced while package.json lagged. | `sync-plugin-version.mjs` + release workflow now make package.json authoritative for plugin version. | FIXED |

Historical entries above are all `SOURCE_REPO` origin. Audit-tooling defects are tracked separately in `00_TOOLING_INTEGRITY_AUDIT.md` and `00_TOOLING_FAILURE_LEDGER.md`.
