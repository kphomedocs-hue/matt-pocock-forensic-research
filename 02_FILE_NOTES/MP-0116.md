# MP-0116 — skills/engineering/wizard/template.sh

- Status: READ
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `1b3cdf40ad4923a9845fd682653d8cc4510f516e`
- Category: skill-executable-config

## Read evidence
Complete frozen contents fetched through GitHub connector.

## Observations
- Bash library implements terminal UX, browser opening, confirmation gates, visible/hidden input, idempotent `.env` upserts, GitHub secret/variable writes, and a completion summary.
- `ask_secret` hides terminal entry but reads any existing value from `.env`; `write_env` persists values as plaintext `KEY=VALUE` lines.
- `set_secret` pipes the value to `gh secret set`; failures become warnings and are recorded for manual completion rather than failing the script.
- `set_var` similarly degrades to warning/manual work if GitHub CLI/auth is unavailable.
- Example wizard writes both Stripe publishable and secret keys to `.env`, while only the secret is additionally sent to GitHub Actions secrets.
- Runtime/security implication: hidden entry protects terminal echo, not local-at-rest secrecy in `.env`.
