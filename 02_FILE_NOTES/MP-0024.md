# MP-0024 — `AGENTS.md`

- Frozen blob SHA: `681311eb9cf453d0faddf3aacaec7357e97ba8e9`
- Git mode: `120000` (symlink)
- Status: READ
- Evidence: frozen blob fetched directly by SHA.

## Source observation

Blob content is exactly:

`CLAUDE.md`

Therefore `AGENTS.md` is a physical Git blob representing a symlink to `CLAUDE.md`, not an independently maintained governance document.

## Later-phase connection

- Record a `SYMLINK` edge from `AGENTS.md` to `CLAUDE.md` during Phase 3.
- Verify harness behavior around this symlink separately; physical identity does not itself prove consumer behavior.
