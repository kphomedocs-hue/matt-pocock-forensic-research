# MP-0031 — docs/engineering/code-review.md

- Status: READ
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `46353162627210af65296c6dee62e422a3400373`
- Read coverage: lines 1–90 and 91–180; second range reached EOF.

## Source facts

- Review has two independent axes: Standards and Spec; neither is allowed to mask the other.
- Fixed point is mandatory and validated before subagents run.
- Diff semantics are `<fixed-point>...HEAD`, so staged/working-tree changes are invisible.
- Standards uses repo rules plus a twelve-smell Fowler baseline; repo rules override the baseline.
- Spec source search order is commit issue references, explicit path, matching spec file, then ask user.
- With no spec, Spec axis is skipped rather than inferred.
- Human docs explicitly warn that subagents may recursively invoke code-review and explode agent count because delegation is not forbidden in their briefs.
- Human docs explicitly warn aggregated findings are not reverified by the parent.
- Docs recommend a fresh review session for independence, while `implement` invokes review inside the authoring flow.

## Later-phase follow-ups

- Compare operative `code-review/SKILL.md` against the recursive-subagent warning and uncommitted-diff limitation.
- Compare `implement` ordering: review-before-commit vs three-dot diff ending at HEAD.
- Runtime-risk candidate: silent omission of uncommitted work.
