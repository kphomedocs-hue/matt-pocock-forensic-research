# MP-0008 — `.changeset/config.json`

Status: **READ**

Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
Blob SHA: `06c7350518b33bdaedd0eae254f75fd887f16963`
Category: changeset

## Full-read evidence

The complete frozen file was inspected on 2026-09-12. Source blob SHA above matches the frozen ledger.

## Source facts

- Uses Changesets config schema 3.1.4.
- Changelog provider is `@changesets/changelog-github` scoped to `mattpocock/skills`.
- `commit` is false.
- Private packages are both versioned and tagged.
- `fixed` and `linked` groups are empty.
- Access is `restricted`.
- Base branch is `main`.
- Internal dependency updates use `patch`.
- Ignore list is empty.

## References / connections to trace

- `package.json`
- `.github/workflows/release.yml`
- `CHANGELOG.md`
- active `.changeset/*.md` files
- Changesets release/tag behavior

## Unresolved / later-phase checks

- Reconcile private-package version/tag behavior with the repository's no-npm-publish release design.
- Verify release workflow uses these settings exactly as intended.
