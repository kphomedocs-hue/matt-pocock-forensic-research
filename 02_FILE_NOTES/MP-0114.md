# MP-0114 — skills/engineering/wizard/SKILL.md

- Status: **CONNECTIONS TRACED**
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `c4294ad8298b9b95fc727496b1b802b1f7363fba`
- Category: skill

## Read evidence
Complete frozen contents fetched through GitHub connector.

## Observations
- Model-invoked skill for human-only manual procedures; explicitly says not to invoke it for steps the agent can perform itself.
- `template.sh` library must be copied unchanged above the STAGES marker; authored work is limited to procedure stages.
- Requires repository exploration first, user confirmation of stages/values, and forbids inventing unknown third-party UI steps.
- Requires static verification (`bash -n`, shellcheck if available) rather than end-to-end execution because the script blocks on human input and opens browsers.
- Ephemeral by default; committed only for repeatable setup paths.

<!-- PHASE3_CONNECTIONS_TRACED_START -->
## Phase 3 connection promotion evidence

- Status promotion: `CONNECTIONS TRACED`.
- Frozen source: `3cca18b368ae95cdbdebbff572ccafa662551015` / blob `c4294ad8298b9b95fc727496b1b802b1f7363fba`.
- Phase 3 closure row: `MP-0114` in `03_PHASE3_CLOSURE_INDEX.json`; gate state `READY_CANDIDATE` with zero blockers.
- Connection graph extraction rules version: `4`.
- Incoming graph edges: **8**; outgoing graph edges: **1**.
- Orphan evaluation: `NON_ORPHAN_BY_INCOMING_EDGE`; closed: **YES**.
- Raw unresolved references originating here: **0**; all applicable raw references reconciled: **YES**.
- Applicable Phase 3 joins (config/distribution/router/invocation/symlink) are enforced by the generated closure index; history bindings are recorded separately and do not imply later-phase verification.
- `CONNECTIONS TRACED` does **not** mean `VERIFIED`; behavior/enforcement, remaining history lineage, contradictions, runtime/distribution observation, second pass, and red-team verification remain later gates where applicable.
<!-- PHASE3_CONNECTIONS_TRACED_END -->
