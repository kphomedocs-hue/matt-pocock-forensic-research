# MP-0027 — `CONTEXT.md`

- Frozen blob SHA: `76ebd23dbb8266dab9491425b39f3c4857fb4a52`
- Status: READ
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Category: root
- Evidence: full frozen file fetched at commit `3cca18b368ae95cdbdebbff572ccafa662551015`.

## Source observations

- Defines the repository's domain language for Issue tracker, Issue, Decision ticket, and Triage role.
- `Issue tracker` is the hosting system; aliases such as backlog manager/backend are explicitly discouraged.
- `Issue` is a tracked unit of work; plain `ticket` is discouraged except when quoting external systems or for `Decision ticket`.
- `Decision ticket` is a wayfinder child Issue whose resolution is a decision rather than an implementation slice.
- `Triage role` is a canonical state-machine role mapped to real tracker labels via per-repo config.
- Ambiguity around "backlog" was explicitly resolved in favor of `Issue tracker`.

## Later-phase connections to trace

- Current usage of these terms across wayfinder, triage, to-tickets, setup, docs and README.
- Whether stale `ticket`/`backlog` usages remain outside deliberate exceptions.
