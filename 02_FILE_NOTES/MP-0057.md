# MP-0057 — scripts/link-skills.sh

- Status: READ
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `558fae823c0e296ad0eff528f827142600a18135`
- Category: repo-executable
- Runtime risk: HIGH

## Read evidence

Complete frozen contents fetched through GitHub connector.

## Observations

- Explicitly maintainer/dev-only and not a supported installer.
- Links skills into both `~/.claude/skills` and `~/.agents/skills`.
- Includes engineering, productivity, and in-progress skills; excludes deprecated and misc.
- Uses symlinks back into the repository so `git pull` updates linked skills.
- Safety check aborts if an entire destination directory is itself a symlink resolving into this repo.
- For an existing per-skill target that is not a symlink, the script executes `rm -rf "$target"` before creating the symlink. This is destructive replacement behavior and merits explicit runtime-risk treatment.

## Later-phase candidates

- Test collision/destructive scenarios in an isolated temp HOME.
- Reconcile exact 33-link set against census and current `main` change #1025 history.
