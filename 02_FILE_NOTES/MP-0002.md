# MP-0002 — `.agents/adr/0002-ship-as-a-claude-code-plugin.md`

Status: **CONNECTIONS TRACED**
Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
Blob SHA: `263d10290d60ea15119d2d3c51af5f4f08b5f178`
Category: governance

## Full-read evidence

The complete frozen file was inspected on 2026-09-12. Source blob SHA above matches the frozen ledger.

## Source facts

- Native Claude Code plugin is shipped; native Codex plugin is deferred.
- Claude plugin can explicitly list promoted skill directories; Codex plugin selection was documented here as a single recursive path, incompatible with the bucketed promoted/non-promoted layout.
- Two rejected Codex workarounds are recorded: point at all `skills/` (would include non-promoted buckets) or curate symlinks (documented as dropped by Codex install copying).
- `skills.sh` remains the universal non-Claude install path.
- Plugin invariants include promoted-set completeness and plugin-version alignment with `package.json`.
- 2026-08-05 update records acceptance into Claude Code's official marketplace and says `.claude-plugin/marketplace.json` is retained only as a direct-repo/fork fallback.
- The official marketplace listing pins a repository SHA, so release visibility to installed users can lag source/main state.

## References / connections to trace

- `.claude-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `package.json`
- `.agents/install-block.md`
- `scripts/link-skills.sh`
- promoted/non-promoted bucket READMEs
- external official Claude marketplace listing
- skills.sh behavior

## Unresolved / later-phase checks

- Verify current plugin manifest against promoted set.
- Verify version-sync enforcement and release workflow.
- Separate source correctness, plugin-package correctness, official-marketplace pin state, and installed-user visibility as distinct runtime/distribution states.
- Recheck whether Codex plugin format assumptions remain true only for the frozen audit epoch; do not project them beyond this snapshot without current evidence.

<!-- PHASE3_CONNECTIONS_TRACED_START -->
## Phase 3 connection promotion evidence

- Status promotion: `CONNECTIONS TRACED`.
- Frozen source: `3cca18b368ae95cdbdebbff572ccafa662551015` / blob `263d10290d60ea15119d2d3c51af5f4f08b5f178`.
- Phase 3 closure row: `MP-0002` in `03_PHASE3_CLOSURE_INDEX.json`; gate state `READY_CANDIDATE` with zero blockers.
- Connection graph extraction rules version: `4`.
- Incoming graph edges: **3**; outgoing graph edges: **6**.
- Orphan evaluation: `NON_ORPHAN_BY_INCOMING_EDGE`; closed: **YES**.
- Raw unresolved references originating here: **0**; all applicable raw references reconciled: **YES**.
- Applicable Phase 3 joins (config/distribution/router/invocation/symlink) are enforced by the generated closure index; history bindings are recorded separately and do not imply later-phase verification.
- `CONNECTIONS TRACED` does **not** mean `VERIFIED`; behavior/enforcement, remaining history lineage, contradictions, runtime/distribution observation, second pass, and red-team verification remain later gates where applicable.
<!-- PHASE3_CONNECTIONS_TRACED_END -->
