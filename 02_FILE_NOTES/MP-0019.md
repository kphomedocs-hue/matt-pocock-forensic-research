# MP-0019 — `.github/workflows/release.yml`

Status: **CONNECTIONS TRACED**
Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
Blob SHA: `b503eff7b073afe8a08d0b25cc31efca78caea02`
Category: ci-release

## Full-read evidence

The complete frozen file was inspected on 2026-09-12. Source blob SHA above matches the frozen ledger.

## Source facts

- Release workflow runs on pushes to `main`.
- Uses one `Version` job on `ubuntu-latest` with content and pull-request write permissions.
- Checks out with `actions/checkout@v4` and uses Node 22.
- Installs dependencies with `npm ci`.
- Uses `changesets/action@v1` to create/version release PRs.
- Version command is `npm run version`.
- Publish action is `npx changeset tag`; this repository is not npm-publishing the private package here.
- No explicit step in this workflow validates promoted-set/plugin symmetry, frontmatter YAML, router completeness, docs mirroring, invocation metadata symmetry, internal link resolution, or no-em-dash policy before version/tagging.

## References / connections to trace

- `package.json` scripts
- `package-lock.json`
- `.changeset/config.json`
- `scripts/sync-plugin-version.mjs`
- `CHANGELOG.md`
- repository-level invariants in `CLAUDE.md`

## Unresolved / later-phase checks

- Determine which checks, if any, are indirectly run by `npm ci`, Changesets, or other repository mechanisms.
- Treat missing explicit invariant checks as an enforcement-gap candidate, not automatically as a runtime bug.
- Inspect workflow/run history for observed failures or versioning behavior where useful.

<!-- PHASE3_CONNECTIONS_TRACED_START -->
## Phase 3 connection promotion evidence

- Status promotion: `CONNECTIONS TRACED`.
- Frozen source: `3cca18b368ae95cdbdebbff572ccafa662551015` / blob `b503eff7b073afe8a08d0b25cc31efca78caea02`.
- Phase 3 closure row: `MP-0019` in `03_PHASE3_CLOSURE_INDEX.json`; gate state `READY_CANDIDATE` with zero blockers.
- Connection graph extraction rules version: `4`.
- Incoming graph edges: **0**; outgoing graph edges: **0**.
- Orphan evaluation: `NON_ORPHAN_BY_STRUCTURAL_ROLE:GITHUB_ACTIONS_ENTRYPOINT`; closed: **YES**.
- Raw unresolved references originating here: **0**; all applicable raw references reconciled: **YES**.
- Applicable Phase 3 joins (config/distribution/router/invocation/symlink) are enforced by the generated closure index; history bindings are recorded separately and do not imply later-phase verification.
- `CONNECTIONS TRACED` does **not** mean `VERIFIED`; behavior/enforcement, remaining history lineage, contradictions, runtime/distribution observation, second pass, and red-team verification remain later gates where applicable.
<!-- PHASE3_CONNECTIONS_TRACED_END -->
