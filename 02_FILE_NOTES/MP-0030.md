# MP-0030 — `docs/engineering/ask-matt.md`

- Frozen blob SHA: `67d1e46192cc22f2f93f6d463dbd831ce174518e`
- Status: READ
- Evidence: complete frozen reread at commit `3cca18b368ae95cdbdebbff572ccafa662551015` in fixed windows: lines 1–80, 81–160; lines 161–220 returned empty.

## Source observations

- `ask-matt` is described as a standalone router over this repo's promoted skills, not a scanner of whatever skills are locally installed.
- It recommends and stops; it should not execute the named skill itself.
- It frames routes as flows with main-flow, on-ramp, standalone and vocabulary-layer categories.
- It carries an explicit phase-boundary decision tree: Continue → `/clear` → `handoff` → subagent → `/compact`.
- It explicitly calls itself a secondary source and says the target `SKILL.md` wins on disagreement.

## Confirmed current stale quantitative statement

- The docs say: "Thirteen of the plugin's twenty-two skills carry the flag" in the known missing-installed-skills discussion.
- Frozen plugin manifest MP-0018 lists 25 promoted skills; prior metadata census established 14 user-invoked / 11 model-invoked.
- This directly supports CT-002 as DOCS STALE.

## Confirmed docs-vs-operative enforcement candidate

- `It's working if` says: "Any claim it makes about another skill's behaviour shows up in the trace as it reading that skill's `SKILL.md`."
- The docs also describe a real failure mode where router summaries were used without opening the target skill.
- Whether the operative router prompt enforces this must be reconciled against `skills/engineering/ask-matt/SKILL.md` at its deterministic MP-ID; prior evidence suggests it does not.
- This directly supports CT-003 pending operative-file durable reread.

## Later-phase connections to trace

- Exact current router coverage vs promoted/user-reachable skill inventory.
- Target `SKILL.md` verification behavior in operative router source.
- Historical router-drift PRs/issues cited by changelog/docs.
