# MP-0055 — package-lock.json

- Status: **CONNECTIONS TRACED**
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `db12f086c92da55b5d724f0dd3afecfebd2d793f`
- Category: dependency-lock
- Runtime risk: HIGH

## Read evidence

Complete frozen contents read in fixed windows through the closing JSON brace because broad fetches truncated.

## Observations

- Root package name is `mattpocock-skills` and root version is `0.0.0` in both the top-level lock metadata and `packages[""]`.
- Lockfile version is 3.
- Root devDependencies are `@changesets/changelog-github` and `@changesets/cli`; the remainder is their transitive dependency graph.
- The lockfile contains release/change-management tooling only; no application/runtime dependency surface is present.
- This directly confirms the frozen-state side of CT-004: package-lock root version is stale relative to package.json 1.2.3.

## Later-phase candidates

- Runtime-test whether `npm ci` and Changesets behavior are unaffected by stale root version metadata.
- Reconcile historical path evidence showing package-lock was only created once and never regenerated.

<!-- PHASE3_CONNECTIONS_TRACED_START -->
## Phase 3 connection promotion evidence

- Status promotion: `CONNECTIONS TRACED`.
- Frozen source: `3cca18b368ae95cdbdebbff572ccafa662551015` / blob `db12f086c92da55b5d724f0dd3afecfebd2d793f`.
- Phase 3 closure row: `MP-0055` in `03_PHASE3_CLOSURE_INDEX.json`; gate state `READY_CANDIDATE` with zero blockers.
- Connection graph extraction rules version: `4`.
- Incoming graph edges: **1**; outgoing graph edges: **1**.
- Orphan evaluation: `NON_ORPHAN_BY_INCOMING_EDGE`; closed: **YES**.
- Raw unresolved references originating here: **0**; all applicable raw references reconciled: **YES**.
- Applicable Phase 3 joins (config/distribution/router/invocation/symlink) are enforced by the generated closure index; history bindings are recorded separately and do not imply later-phase verification.
- `CONNECTIONS TRACED` does **not** mean `VERIFIED`; behavior/enforcement, remaining history lineage, contradictions, runtime/distribution observation, second pass, and red-team verification remain later gates where applicable.
<!-- PHASE3_CONNECTIONS_TRACED_END -->
