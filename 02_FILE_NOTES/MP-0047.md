# MP-0047 — docs/engineering/wizard.md

- Status: READ
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `84e047b2481c2afe4e5247231a8600d9721f1b88`
- Category: human-docs

## Read evidence

Complete frozen contents fetched through GitHub connector.

## Observations

- `wizard` generates an interactive bash program for human-only steps; the agent authors but does not run it.
- Scoping reads env/config/workflow references, presents an ordered stage list, and only then writes the script.
- Secrets are intended to be captured locally by the script rather than placed in model context.
- The template is treated as a fixed runtime library; generated work should customize stages only.
- Static verification (`bash -n`, optional shellcheck, destination tracing) substitutes for end-to-end execution.
- Ephemeral by default; commit only when the procedure is genuinely reusable.
- Docs acknowledge no back button and readline/arrow-key limitations.

## Later-phase candidates

- Verify template behavior, quoting, secret handling, and idempotent env writes against executable source.
- Reconcile docs references to time/progress behavior against v1.2.3 history that removed time estimates.
