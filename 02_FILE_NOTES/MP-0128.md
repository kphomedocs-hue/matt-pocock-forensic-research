# MP-0128 — skills/in-progress/setup-ts-deep-modules/dependency-cruiser.config.cjs

- Status: READ
- Frozen commit: `3cca18b368ae95cdbdebbff572ccafa662551015`
- Blob SHA: `54030ede7d36b7f72fc0f645fa414efa9b6f63ed`
- Category: skill-executable-config

## Read evidence
Complete frozen contents fetched through GitHub connector.

## Observations
- Enforces package-root entry-point boundaries using dependency-cruiser with `PACKAGES_ROOT = "src/packages"` as the only expected customization.
- Root files are public entry points; any nested subfolder path is treated as private internals.
- Contains five error-level rules: app→internals blocked, cross-package internals blocked, tests→internals blocked except own test fixtures, tests folder private from non-tests, and no cycles.
- Uses regex capture `$1` to allow same-package internal imports while blocking cross-package deep imports.
- Layering is intentionally left as an optional separate concern.
